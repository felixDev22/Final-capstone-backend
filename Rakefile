require_relative 'config/application'

Rails.application.load_tasks

namespace :assets do
  task :precompile do
    # Task implementation goes here
    # You can add any necessary code to precompile assets
    # For example, if you are using the AWS SDK for Ruby (aws-sdk-s3 gem) to generate a presigned URL,
    # you can add the code here.
  end
end

